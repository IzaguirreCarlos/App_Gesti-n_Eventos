services:
  - type: web
    name: eventpro-web
    env: python
    buildCommand: "./build.sh"
    startCommand: "./start.sh"
    healthCheckPath: /api/schema/
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: eventpro-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: eventpro-redis
          type: redis
          property: connectionString
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: SECURE_SSL_REDIRECT
        value: "True"
      - key: EMAIL_BACKEND
        value: "django.core.mail.backends.smtp.EmailBackend"
      - key: EMAIL_HOST
        value: "smtp.gmail.com"
      - key: EMAIL_PORT
        value: "587"
      - key: EMAIL_USE_TLS
        value: "True"
      - key: EMAIL_HOST_USER
        sync: false
      - key: EMAIL_HOST_PASSWORD
        sync: false
      - key: DEFAULT_FROM_EMAIL
        value: "EventPro <noreply@eventpro.app>"
      - key: PYTHON_VERSION
        value: "3.12.0"

  - type: worker
    name: eventpro-celery
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "celery -A config worker -l INFO --concurrency 2"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: SECRET_KEY
        fromService:
          name: eventpro-web
          type: web
          envVarKey: SECRET_KEY
      - key: DATABASE_URL
        fromDatabase:
          name: eventpro-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: eventpro-redis
          type: redis
          property: connectionString

  - type: worker
    name: eventpro-celery-beat
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "celery -A config beat -l INFO --scheduler django_celery_beat.schedulers:DatabaseScheduler"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: SECRET_KEY
        fromService:
          name: eventpro-web
          type: web
          envVarKey: SECRET_KEY
      - key: DATABASE_URL
        fromDatabase:
          name: eventpro-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: eventpro-redis
          type: redis
          property: connectionString

databases:
  - name: eventpro-db
    databaseName: eventpro
    user: eventpro
    plan: free

  - name: eventpro-redis
    type: redis
    plan: free
    ipAllowList: []
